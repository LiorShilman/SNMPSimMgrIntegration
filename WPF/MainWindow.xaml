<Window x:Class="SNMPSimMgr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SNMPSimMgr Integration" Height="300" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="MIB Panel Integration" FontSize="20" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="SignalR server running on port 5050" FontSize="14"
                       HorizontalAlignment="Center" Foreground="Gray"/>
            <TextBlock Text="Open http://localhost:4300 in a browser" FontSize="12"
                       HorizontalAlignment="Center" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</Window>
